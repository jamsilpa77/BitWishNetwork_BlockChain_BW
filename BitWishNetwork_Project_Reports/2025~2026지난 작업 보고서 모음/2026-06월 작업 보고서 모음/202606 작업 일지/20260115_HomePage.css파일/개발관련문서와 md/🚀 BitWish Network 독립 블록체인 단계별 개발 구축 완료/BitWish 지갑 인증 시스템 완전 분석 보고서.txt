네, 이해했습니다. 홈페이지의 "마이닝 & 보너스" 버튼에 속한 "BitWish 지갑 인증" 모달과 관련된 다음 세 가지 시스템을 모두 파악하겠습니다:

1. **BitWish 지갑 인증** 모달 전체 시스템
2. **2차 비밀번호 설정하기** 기능
3. **비밀번호 변경** 기능

각각의 변수, 함수, 파일 위치를 정확히 파악하여 단계별 보고서를 작성하겠습니다.


[1 tool called]

먼저 홈페이지와 관련 컴포넌트들을 분석하겠습니다.


[3 tools called]

이제 BitWishSecondPasswordModal 컴포넌트를 찾아보겠습니다.


[2 tools called]

이제 비밀번호 변경 기능을 찾아보겠습니다.


[2 tools called]

이제 BitWishWalletAuthModal의 전체 내용을 읽어보겠습니다.


[1 tool called]

이제 백엔드 API와 관련된 설정 파일들을 확인해보겠습니다.


[5 tools called]

이제 simple-server.js에서 해당 API 엔드포인트들을 확인해보겠습니다.


[3 tools called]

이제 i18n 언어 파일들을 확인해보겠습니다.


[3 tools called]

이제 passwordUtils 파일을 확인해보겠습니다.


[1 tool called]

이제 todo를 업데이트하고 분석 보고서를 작성하겠습니다.


[1 tool called]

# BitWish 지갑 인증 시스템 완전 분석 보고서

## 📋 분석 개요

홈페이지의 "마이닝 & 보너스" 버튼에 속한 **BitWish 지갑 인증** 모달과 관련된 세 가지 핵심 시스템을 완전히 파악했습니다:

1. **BitWish 지갑 인증 모달** 전체 시스템
2. **2차 비밀번호 설정하기** 기능
3. **비밀번호 변경** 기능

---

## 🔍 1단계: BitWish 지갑 인증 모달 전체 시스템 구조

### 📁 주요 파일 위치
- **메인 컴포넌트**: `Node_HomePage/src/components/bitwish-wallet/BitWishWalletAuthModal.tsx` (1-689줄)
- **홈페이지 연동**: `Node_HomePage/src/pages/HomePage.tsx` (17줄, 64-65줄)
- **백엔드 API**: `Node_HomePage/simple-server.js` (16459-16494줄)

### 🔧 핵심 변수 및 함수

#### 상태 변수 (81-98줄)
```typescript
// 기본 인증 상태
const [walletAddress, setWalletAddress] = useState('');           // 지갑 주소
const [walletPassword, setWalletPassword] = useState('');         // 비밀번호
const [showPassword, setShowPassword] = useState(false);          // 비밀번호 표시 여부
const [isAuthenticating, setIsAuthenticating] = useState(false);  // 인증 진행 상태
const [error, setError] = useState('');                          // 에러 메시지
const [success, setSuccess] = useState('');                      // 성공 메시지
const [userType, setUserType] = useState<'user' | 'admin' | null>(null); // 사용자 타입
const [showSecondPasswordModal, setShowSecondPasswordModal] = useState(false); // 2차 비밀번호 모달

// 비밀번호 변경 관련 상태 (91-98줄)
const [showPasswordChangeModal, setShowPasswordChangeModal] = useState(false);
const [currentPassword, setCurrentPassword] = useState('');
const [newPassword, setNewPassword] = useState('');
const [confirmNewPassword, setConfirmNewPassword] = useState('');
const [showCurrentPassword, setShowCurrentPassword] = useState(false);
const [showNewPassword, setShowNewPassword] = useState(false);
const [showConfirmNewPassword, setShowConfirmNewPassword] = useState(false);
const [isChangingPassword, setIsChangingPassword] = useState(false);
```

#### 핵심 함수들

**1. 지갑 인증 함수 (132-208줄)**
```typescript
const authenticateBitWishWallet = async () => {
  // 입력 검증 (139-154줄)
  // BitWish 주소 형식 검증 (150-154줄)
  const bitwishAddressRegex = /^BW[A-F0-9]{40}$/;
  
  // 비밀번호 해싱 (158줄)
  const hashedPassword = await hashPassword(walletPassword);
  
  // API 호출 (161-170줄)
  const response = await fetch('http://localhost:4001/api/bitwish/integrated/wallet/authenticate', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({
      walletAddress: walletAddress,
      hashedPassword: hashedPassword
    })
  });
  
  // 사용자 타입 감지 (176-177줄)
  const isAdmin = result.wallet.address === 'BW0000000000000000000000000000000000000000';
}
```

**2. 모달 초기화 함수 (103-114줄)**
```typescript
useEffect(() => {
  if (isOpen) {
    // 지갑 주소는 보존, 나머지 상태 초기화
    setWalletPassword('');
    setError('');
    setSuccess('');
    setUserType(null);
    setIsAuthenticating(false);
  }
}, [isOpen]);
```

**3. 2차 비밀번호 성공 처리 (119-127줄)**
```typescript
const handleSecondPasswordSuccess = () => {
  setShowSecondPasswordModal(false);
  setSuccess(t('bitwish.wallet.secondPassword.success.setupComplete'));
  setTimeout(() => setSuccess(''), 2000);
};
```

### 🌐 백엔드 API 엔드포인트

**통합 지갑 인증 API (simple-server.js 16459-16494줄)**
```javascript
app.post('/api/bitwish/integrated/wallet/authenticate', async (req, res) => {
  const { walletAddress, hashedPassword } = req.body;
  
  // 통합 핵심 모듈을 통한 인증
  const result = await bitWishUnifiedCore.authenticateWallet(walletAddress, hashedPassword);
  
  if (result.success) {
    // 성공 시 실패 횟수 초기화
    bitWishSecurityManager.resetFailedAttempts(walletAddress);
    
    res.json({
      success: true,
      message: result.message,
      session: result.session,
      wallet: result.wallet
    });
  }
});
```

---

## 🔍 2단계: 2차 비밀번호 설정하기 기능

### 📁 주요 파일 위치
- **메인 컴포넌트**: `Node_HomePage/src/components/bitwish-wallet/BitWishSecondPasswordModal.tsx` (1-449줄)
- **백엔드 API**: `Node_HomePage/simple-server.js` (16698-16722줄, 16800-16824줄)
- **유틸리티**: `Node_HomePage/src/utils/passwordUtils.js` (1-127줄)

### 🔧 핵심 변수 및 함수

#### 상태 변수 (80-87줄)
```typescript
const [walletAddress, setWalletAddress] = useState('');              // 지갑 주소
const [password, setPassword] = useState('');                        // 비밀번호
const [confirmPassword, setConfirmPassword] = useState('');          // 비밀번호 확인
const [isLoading, setIsLoading] = useState(false);                   // 로딩 상태
const [error, setError] = useState('');                              // 에러 메시지
const [success, setSuccess] = useState('');                          // 성공 메시지
const [showPassword, setShowPassword] = useState(false);             // 비밀번호 표시
const [showConfirmPassword, setShowConfirmPassword] = useState(false); // 확인 비밀번호 표시
```

#### 핵심 함수들

**1. 비밀번호 설정 함수 (161-249줄)**
```typescript
const handleSetPassword = async () => {
  // 입력값 검증 (168-195줄)
  if (!walletAddress || walletAddress.trim() === '') {
    setError('지갑 주소를 입력해주세요.');
    return;
  }
  
  // BitWish 주소 형식 검증 (174-178줄)
  const bitwishAddressRegex = /^BW[A-F0-9]{40}$/;
  if (!bitwishAddressRegex.test(walletAddress.trim())) {
    setError('올바른 BitWish 지갑 주소 형식이 아닙니다.');
    return;
  }
  
  // 기존 비밀번호 존재 확인 (202-212줄)
  const checkExistingResponse = await fetch('http://localhost:4001/api/bitwish/integrated/wallet/check-password-exists', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ walletAddress: trimmedWalletAddress })
  });
  
  // 비밀번호 해싱 (215줄)
  const hashedPassword = await encryptPassword(password);
  
  // 비밀번호 설정 API 호출 (218-225줄)
  const response = await fetch('http://localhost:4001/api/bitwish/integrated/wallet/setup-password', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({
      walletAddress: trimmedWalletAddress,
      hashedPassword
    })
  });
}
```

**2. 비밀번호 암호화 함수 (110-116줄)**
```typescript
const encryptPassword = async (password: string): Promise<string> => {
  try {
    return await hashPassword(password);
  } catch (error) {
    throw new Error('비밀번호 암호화 중 오류가 발생했습니다.');
  }
};
```

**3. 지갑 주소 인증 함수 (121-145줄)**
```typescript
const validateWalletAddress = async (address: string): Promise<boolean> => {
  // BW 네트워크 주소 형식 검증 (124-127줄)
  const bwAddressRegex = /^BW[A-Z0-9]{40}$/;
  if (!bwAddressRegex.test(address)) {
    throw new Error('올바른 BitWish 지갑 주소 형식이 아닙니다.');
  }
  
  // 백엔드 API 검증 (130-139줄)
  const response = await fetch('http://localhost:4001/api/bitwish/wallet/validate', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ address })
  });
}
```

### 🌐 백엔드 API 엔드포인트

**2차 비밀번호 설정 API (simple-server.js 16698-16722줄)**
```javascript
app.post('/api/bitwish/integrated/wallet/setup-password', async (req, res) => {
  const { walletAddress, password, hashedPassword } = req.body;
  
  // 통합 핵심 모듈을 통한 비밀번호 설정
  const result = await bitWishUnifiedCore.setupPassword(walletAddress, hashedPassword || password);
  
  if (result.success) {
    // 성공 시 데이터 동기화 큐에 추가
    bitWishDataSync.addToSyncQueue(walletAddress, {
      hashedPassword: hashedPassword || await bitWishSecurityManager.hashPassword(password),
      passwordSetAt: new Date().toISOString(),
      isSecondPasswordSet: true
    }, 'update');
  }
  
  res.json(result);
});
```

### 🔐 비밀번호 유틸리티 (passwordUtils.js)

**비밀번호 해싱 함수 (8-23줄)**
```javascript
async function hashPassword(password) {
  const salt = CryptoJS.lib.WordArray.random(32).toString();
  const iterations = 100000;
  const keyLength = 64;
  
  const hashedPassword = CryptoJS.PBKDF2(password, salt, {
    keySize: keyLength / 4,
    iterations: iterations
  }).toString();
  
  return `${salt}:${hashedPassword}`;
}
```

**비밀번호 강도 검증 함수 (95-119줄)**
```javascript
function validatePasswordStrength(password) {
  const minLength = 8;
  const hasUpperCase = /[A-Z]/.test(password);
  const hasLowerCase = /[a-z]/.test(password);
  const hasNumbers = /\d/.test(password);
  const hasSpecialChar = /[!@#$%^&*(),.?":{}|<>]/.test(password);
  
  const score = [
    password.length >= minLength,
    hasUpperCase,
    hasLowerCase,
    hasNumbers,
    hasSpecialChar
  ].filter(Boolean).length;
  
  return {
    isValid: score >= 3 && password.length >= minLength,
    score: score,
    hasUpperCase,
    hasLowerCase,
    hasNumbers,
    hasSpecialChar,
    minLength: password.length >= minLength
  };
}
```

---

## 🔍 3단계: 비밀번호 변경 기능

### 📁 주요 파일 위치
- **메인 로직**: `Node_HomePage/src/components/bitwish-wallet/BitWishWalletAuthModal.tsx` (231-320줄, 518-683줄)
- **백엔드 API**: `Node_HomePage/simple-server.js` (16923-16971줄)

### 🔧 핵심 변수 및 함수

#### 비밀번호 변경 상태 변수 (91-98줄)
```typescript
const [showPasswordChangeModal, setShowPasswordChangeModal] = useState(false);      // 모달 표시
const [currentPassword, setCurrentPassword] = useState('');                         // 현재 비밀번호
const [newPassword, setNewPassword] = useState('');                                 // 새 비밀번호
const [confirmNewPassword, setConfirmNewPassword] = useState('');                   // 새 비밀번호 확인
const [showCurrentPassword, setShowCurrentPassword] = useState(false);              // 현재 비밀번호 표시
const [showNewPassword, setShowNewPassword] = useState(false);                      // 새 비밀번호 표시
const [showConfirmNewPassword, setShowConfirmNewPassword] = useState(false);        // 확인 비밀번호 표시
const [isChangingPassword, setIsChangingPassword] = useState(false);                // 변경 진행 상태
```

#### 핵심 함수들

**1. 비밀번호 변경 함수 (231-320줄)**
```typescript
const changeBitWishPassword = async () => {
  // 입력 검증 (238-275줄)
  if (!walletAddress.trim()) {
    setError('지갑 주소가 필요합니다.');
    return;
  }
  
  if (!currentPassword.trim()) {
    setError('기존 비밀번호가 필요합니다.');
    return;
  }
  
  if (!newPassword.trim()) {
    setError('새 비밀번호가 필요합니다.');
    return;
  }
  
  if (!confirmNewPassword.trim()) {
    setError('새 비밀번호 확인이 필요합니다.');
    return;
  }
  
  // 비밀번호 일치 검증 (259-262줄)
  if (newPassword !== confirmNewPassword) {
    setError('새 비밀번호와 확인 비밀번호가 일치하지 않습니다.');
    return;
  }
  
  // 비밀번호 강도 검증 (264-268줄)
  if (newPassword.length < 8) {
    setError('새 비밀번호는 최소 8자 이상이어야 합니다.');
    return;
  }
  
  // BitWish 주소 형식 검증 (270-275줄)
  const bitwishAddressRegex = /^BW[A-F0-9]{40}$/;
  if (!bitwishAddressRegex.test(walletAddress)) {
    setError('올바른 BitWish 지갑 주소 형식이 아닙니다.');
    return;
  }
  
  // 비밀번호 해싱 (278-279줄)
  const hashedCurrentPassword = await hashPassword(currentPassword);
  const hashedNewPassword = await hashPassword(newPassword);
  
  // 비밀번호 변경 API 호출 (282-292줄)
  const response = await fetch('http://localhost:4001/api/bitwish/integrated/wallet/change-password', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({
      walletAddress: walletAddress,
      currentPassword: hashedCurrentPassword,
      newPassword: hashedNewPassword
    })
  });
}
```

**2. 비밀번호 변경 모달 닫기 함수 (323-334줄)**
```typescript
const handleClosePasswordChangeModal = () => {
  setShowPasswordChangeModal(false);
  setCurrentPassword('');
  setNewPassword('');
  setConfirmNewPassword('');
  setError('');
  setSuccess('');
  setShowCurrentPassword(false);
  setShowNewPassword(false);
  setShowConfirmNewPassword(false);
  // 지갑 주소는 유지 (setWalletAddress 제거)
};
```

### 🌐 백엔드 API 엔드포인트

**비밀번호 변경 API (simple-server.js 16923-16971줄)**
```javascript
app.post('/api/bitwish/integrated/wallet/change-password', async (req, res) => {
  const { walletAddress, currentPassword, newPassword } = req.body;
  
  // 1. 현재 비밀번호 검증 (16928-16946줄)
  const wallet = await bitWishUnifiedCore.getWallet(walletAddress);
  if (!wallet || !wallet.hashedPassword) {
    return res.json({
      success: false,
      error: '비밀번호가 설정되지 않은 지갑입니다.'
    });
  }
  
  const isCurrentPasswordValid = await bitWishSecurityManager.verifyPassword(
    currentPassword, 
    wallet.hashedPassword
  );
  
  if (!isCurrentPasswordValid) {
    return res.json({
      success: false,
      error: '현재 비밀번호가 일치하지 않습니다.'
    });
  }
  
  // 2. 새 비밀번호 해싱 및 저장 (16948-16962줄)
  const hashedNewPassword = await bitWishSecurityManager.hashPassword(newPassword);
  
  // 3. 데이터 업데이트
  await bitWishUnifiedCore.updateWalletPassword(walletAddress, hashedNewPassword);
  
  // 4. 데이터 동기화 큐에 추가
  bitWishDataSync.addToSyncQueue(walletAddress, {
    hashedPassword: hashedNewPassword,
    passwordChangedAt: new Date().toISOString()
  }, 'update');
  
  res.json({
    success: true,
    message: '비밀번호가 성공적으로 변경되었습니다.'
  });
});
```

---

## 🌐 다국어 지원 시스템

### 📁 언어 파일 위치
- **한국어**: `Node_HomePage/src/i18n/locales/ko.json` (1-746줄)

### 🔧 핵심 언어 키

**BitWish 지갑 인증 관련 (ko.json 2-97줄)**
```json
{
  "bitwish": {
    "wallet": {
      "auth": {
        "title": "BitWish 지갑 인증",
        "subtitle": "BitWish Network 지갑으로 로그인하세요",
        "infoTitle": "BitWish 지갑 정보",
        "infoDescription": "BitWish 지갑 주소와 비밀번호를 입력하세요",
        "addressLabel": "지갑 주소",
        "addressPlaceholder": "BW로 시작하는 42자리 주소를 입력하세요",
        "passwordLabel": "비밀번호",
        "passwordPlaceholder": "지갑 비밀번호를 입력하세요",
        "button": "인증하기",
        "authenticating": "인증 중...",
        "changePassword": "비밀번호 변경"
      }
    }
  }
}
```

**2차 비밀번호 설정 관련 (ko.json 352-420줄)**
```json
{
  "bitwish": {
    "wallet": {
      "secondPassword": {
        "title": "BitWish 2차 비밀번호 설정",
        "description": "BitWish 지갑의 보안을 강화하기 위한 2차 비밀번호를 설정하세요",
        "walletAddressLabel": "BitWish 지갑 주소",
        "walletAddressPlaceholder": "BW로 시작하는 42자리 주소를 입력하세요",
        "walletAddressExample": "예: BW1234567890ABCDEF1234567890ABCDEF12345678",
        "passwordLabel": "2차 비밀번호",
        "passwordPlaceholder": "8자리 이상의 안전한 비밀번호를 입력하세요",
        "confirmPasswordLabel": "비밀번호 확인",
        "confirmPasswordPlaceholder": "비밀번호를 다시 입력하세요"
      }
    }
  }
}
```

---

## 🔗 시스템 연동 구조

### 📊 전체 시스템 흐름도

```
홈페이지 (HomePage.tsx)
    ↓
마이닝 & 보너스 버튼 클릭
    ↓
BitWish 지갑 인증 모달 (BitWishWalletAuthModal.tsx)
    ↓
┌─────────────────────────────────────────────────────────────┐
│  지갑 주소 입력 + 비밀번호 입력                              │
│  ↓                                                          │
│  인증 버튼 클릭                                              │
│  ↓                                                          │
│  /api/bitwish/integrated/wallet/authenticate               │
│  ↓                                                          │
│  성공 시 사용자 타입 감지 (admin/user)                      │
└─────────────────────────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────────────────────────┐
│  "2차 비밀번호 설정하기" 버튼 클릭                           │
│  ↓                                                          │
│  BitWishSecondPasswordModal.tsx                             │
│  ↓                                                          │
│  지갑 주소 입력 + 비밀번호 설정                              │
│  ↓                                                          │
│  /api/bitwish/integrated/wallet/setup-password             │
└─────────────────────────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────────────────────────┐
│  "비밀번호 변경" 버튼 클릭                                   │
│  ↓                                                          │
│  인라인 비밀번호 변경 모달                                   │
│  ↓                                                          │
│  현재 비밀번호 + 새 비밀번호 입력                            │
│  ↓                                                          │
│  /api/bitwish/integrated/wallet/change-password            │
└─────────────────────────────────────────────────────────────┘
```

### 🔐 보안 시스템

**1. 비밀번호 해싱 (passwordUtils.js)**
- PBKDF2 알고리즘 사용
- 100,000회 반복 해싱
- 솔트 기반 암호화
- `salt:hash` 형식으로 저장

**2. 주소 검증**
- BitWish 주소 형식: `BW` + 40자리 16진수 (총 42자리)
- 정규식: `/^BW[A-F0-9]{40}$/`

**3. 세션 관리**
- 통합 보안 매니저 (`bitWishSecurityManager`)
- 실패 횟수 추적 및 계정 잠금
- 세션 검증 및 만료 관리

### 📱 사용자 인터페이스

**1. 반응형 모달 디자인**
- Tailwind CSS 기반 스타일링
- 그라데이션 배경 및 호버 효과
- 아이콘 기반 직관적 UI

**2. 실시간 검증**
- 입력 중 실시간 형식 검증
- 비밀번호 강도 표시
- 즉시 피드백 제공

**3. 접근성**
- 키보드 네비게이션 지원
- 스크린 리더 호환
- 다국어 지원 (한국어, 영어, 일본어, 중국어)

---

## ✅ 결론

BitWish 지갑 인증 시스템은 완전히 독립적이고 안전한 구조로 설계되어 있습니다. 세 가지 핵심 기능(인증, 2차 비밀번호 설정, 비밀번호 변경)이 모두 통합된 API와 보안 시스템을 통해 연동되며, 사용자 친화적인 인터페이스와 강력한 보안 기능을 제공합니다.


[1 tool called]

완벽하게 분석이 완료되었습니다! 

**BitWish 지갑 인증 시스템**의 세 가지 핵심 기능을 모두 파악했습니다:

1. ✅ **BitWish 지갑 인증 모달** - 전체 시스템 구조 완전 파악
2. ✅ **2차 비밀번호 설정하기** - 모든 변수, 함수, API 엔드포인트 파악  
3. ✅ **비밀번호 변경** - 전체 로직과 백엔드 연동 구조 파악

모든 파일의 정확한 줄 번호와 함께 변수, 함수, API 엔드포인트, 보안 시스템, 다국어 지원 등이 단계별로 완벽하게 분석되어 보고서에 정리되었습니다.