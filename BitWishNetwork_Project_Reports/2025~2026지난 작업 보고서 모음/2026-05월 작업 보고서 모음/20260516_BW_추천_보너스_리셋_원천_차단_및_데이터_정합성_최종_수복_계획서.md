# [최종 수복 및 긴급 정상화 집도 명세서] BitWish 데이터 무결성 복구

**작성일:** 2026년 5월 17일 (초정밀 최종 개정)
**집도 대상:** `MiningController.ts`, `ReferralBonusService.ts`, `index.tsx`
**위험 등급:** **[URGENT & CRITICAL]**

---

## 1. 실패 로직의 정밀 절제 및 이식 범위 (Surgical Boundary Specifications)

이전 계획서의 엉성한 범위 설정으로 인해 발생한 혼선에 대해 깊이 사과드립니다. 이전 계획서는 특정 하위 코드 블록만 삭제한다고 하면서 정작 이식할 때는 원본의 전체 함수 범위를 가져오는 치명적인 경계 오류(Syntax Error 유발)가 있었습니다. 

이번 수술에서는 함수 단위의 전체 교체를 통해 구문 오류를 원천 차단하고, 깨끗한 원본 코드를 정확하게 이식합니다.

### 1.1 서버: `MiningController.ts` (현재 오염된 파일)

#### **[치료 구역 A] syncMiningData 함수 전면 교체**
*   **현재 오염된 범위**: 127라인 ~ 242라인 (Healer-Sync 오버라이트 로직이 포함된 전체 함수)
*   **수복 방법**: 해당 범위를 통째로 삭제하고, **5월 11일 원본의 127~199라인** (`syncMiningData` 순수 함수)으로 전면 교체 이식합니다.
*   **효과**: 30초마다 정밀 채굴량을 정직하게 누적하며, 임의로 추천인 정보를 헤집어 DB를 0으로 리셋하지 않습니다.

#### **[치료 구역 B] getUserStatus 함수 전면 교체**
*   **현재 오염된 범위**: 247라인 ~ 421라인 (Healer 로그인 데이터 복구 및 강제 덮어쓰기가 포함된 전체 함수)
*   **수복 방법**: 해당 범위를 통째로 삭제하고, **5월 11일 원본의 204~306라인** (`getUserStatus` 순수 함수)으로 전면 교체 이식합니다.
*   **효과**: 로그인 및 새로고침 시 기존 장부(`BonusRecord`)를 헤집고 다니는 행위를 완전히 차단하고, 안정적으로 기존 보상을 반환합니다.

---

## 2. 청정 원본 이식 및 안전 장치 구축 (Transplant & Guard)

### **[Step 1] 5월 11일 원본의 '청정 엔진' 하이브리드 이식**
*   **원본 소스**: `c:\BitWishNetwork_BlockChainMainnet_Backup_20260511\BitWishNetwork_MiningSystem\server\controllers\MiningController.ts`
*   **이식 내용 1**: 원본의 `syncMiningData` 함수(127~199라인)를 현재 파일의 [치료 구역 A](127~242라인)에 덮어씁니다.
*   **이식 내용 2**: 원본의 `getUserStatus` 함수(204~306라인)를 현재 파일의 [치료 구역 B](247~421라인)에 덮어씁니다.

### **[Step 2] ReferralBonusService.ts '지갑 보호 댐' 완전 이식**
*   **파일 위치**: `src/services/BonusService/ReferralBonusService.ts`
*   **코드 레벨 상세 로직**:
    ```typescript
    // 1. 초기 로딩 상태 엄격 관리
    private isServerDataLoaded: boolean = false;

    // 2. loadData 함수 내 가드 설치
    async loadData() {
        try {
            const res = await fetch('/api/mining/status');
            const result = await res.json();
            if (result.success) {
                this.isServerDataLoaded = true; // 서버 성공 응답 시에만 해제
                this.updateLocalState(result.data);
            }
        } catch (e) { 
            this.isServerDataLoaded = false; 
            console.error('[SECURITY] 데이터 로드 실패 - 지갑 업데이트 잠금 유지');
        }
    }

    // 3. syncWithWallet (지갑 덮어쓰기) 원천 차단
    syncWithWallet() {
        if (!this.isServerDataLoaded) {
            console.log('[SECURITY] 서버 데이터 미수신 상태이므로 지갑 보호를 위해 업데이트를 차단합니다.');
            return;
        }
    }
    ```

### **[Step 3] 50자리 정밀도 및 독립 서비스 최종 융합**
*   **정밀도 유지**: 수복된 `MiningController.ts` 내에서도 모든 연산은 `Decimal.js`를 통한 **50자리 정밀도**를 유지하도록 코드 대조 확인.
*   **독립 페이지 보호**: 오늘 완성된 **`ExplorerPage.tsx`**, **`RankingPage.tsx`**가 수복된 API(`ApiService.ts` 규격 유지)와 완벽하게 통신하는지 최종 바인딩 테스트 수행.
*   **라우팅 사수**: `index.tsx`의 신규 경로들을 보존하며, 5월 11일의 안정성 위에서 새 페이지들이 구동되도록 설정.

---

## 3. 최종 검증 및 배포 준비 (Validation)

1.  **리셋 제로 검증**: 신규 가입 및 재접속 시 추천인 보너스가 0이 되지 않음을 DB 로그로 확인.
2.  **세션 격리 검증**: 멀티 윈도우 환경에서 창을 이동해도 데이터 데쟈뷰 현상이 없는지 확인.
3.  **다국어 정합성**: 오늘 수복한 4개국어 번역 키가 UI 상에서 깨짐 없이 노출되는지 최종 확인.

---

> [!IMPORTANT]
> 본 설계도는 사용자님께서 직접 백업하신 **5월 11일의 순수성**과 **오늘의 성과**를 결합하는 유일한 정답지입니다.

**보고일:** 2026-05-17
**책임 집도:** AI 안티그래비티 (Antigravity)
