# [작업전보고] 20260115_관리자_마이닝_초기화기능_복구계획서

**작성일시:** 2026년 1월 15일
**작성자:** AI Assistant (Antigravity)
**수신자:** BitWishNetwork 최고 관리자
**주제:** 관리자 페이지 '마이닝 초기화' 기능의 백엔드 실제 구현 계획

---

## 1. 현상 분석 (Root Cause)
*   관리자 페이지에서 **[마이닝 데이터 초기화]** 버튼을 누르면 "성공" 메시지는 뜨지만, 실제 데이터는 변하지 않음.
*   원인: 백엔드 API (`server/routes/admin.ts`) 내부가 **비어있음 (Dummy Logic).** 주석으로만 처리되어 있고 실제 DB 업데이트 코드가 없음.

## 2. 해결 목표
*   특정 지갑 주소 입력 후 초기화 실행 시, **마이닝 시간, 누적 보상, 추천 보너스 등 모든 데이터를 '0'으로 리셋.**
*   마이닝 중단 상태(`isMining: false`)로 강제 전환.

## 3. 구현 내용 (상세 코드)

**대상 파일:** `c:\BitWishNetwork_BlockChainMainnet\BitWishNetwork_MiningSystem\server\routes\admin.ts`

**[수정 코드]**
기존의 비어있는 로직을 아래의 **실제 Mongoose DB 업데이트 로직**으로 교체합니다.

```typescript
// 필요한 모델 임포트 (파일 상단에 추가)
import MiningState from '../models/MiningState';
import BonusRecord from '../models/BonusRecord';

// ... (기존 코드) ...

// POST /api/admin/mining/reset
router.post('/mining/reset', [
    body('walletAddress').notEmpty().withMessage('지갑 주소는 필수입니다')
], async (req: express.Request, res: express.Response) => {
    try {
        const errors = validationResult(req);
        if (!errors.isEmpty()) {
            return res.status(400).json({ success: false, errors: errors.array() });
        }

        const { walletAddress } = req.body;

        console.log(`[Admin] Resetting mining data for wallet: ${walletAddress}`);

        // 1. MiningState 초기화
        // - 채굴 중지, 시작 시간 삭제, 누적 보상 0
        const miningUpdate = await MiningState.findOneAndUpdate(
            { walletAddress },
            { 
                $set: { 
                    isMining: false,
                    miningStartTime: null,
                    lastSyncTime: new Date(),
                    accumulatedReward: "0"
                }
            },
            { new: true }
        );

        // 2. BonusRecord 초기화
        // - 추천 보너스 보관함, 리워드 보관함 0
        // - (주의: 추천인 관계(족보) 자체는 삭제하지 않음. 금액만 리셋)
        const bonusUpdate = await BonusRecord.findOneAndUpdate(
            { walletAddress },
            {
                $set: {
                    referralBonusStorage: "0",
                    referralRewardStorage: "0",
                    lastUpdated: new Date()
                }
            },
            { new: true }
        );

        if (!miningUpdate) {
            return res.status(404).json({ 
                success: false, 
                message: '해당 지갑의 마이닝 상태를 찾을 수 없습니다 (아직 마이닝 시도 안함)' 
            });
        }

        console.log(`[Admin] Reset Complete. MiningState: ${!!miningUpdate}, BonusRecord: ${!!bonusUpdate}`);

        return res.json({
            success: true,
            message: '마이닝 데이터가 성공적으로 초기화되었습니다 (DB 적용 완료)',
            data: {
                walletAddress,
                resetTime: new Date().toISOString(),
                miningState: miningUpdate,
                bonusRecord: bonusUpdate
            }
        });

    } catch (error) {
        console.error('Admin mining reset error:', error);
        return res.status(500).json({
            success: false,
            message: '서버 오류가 발생했습니다: ' + (error as any).message
        });
    }
});
```

## 4. 기대 효과
1.  이 코드가 적용되면, 관리자 페이지에서 초기화 버튼 클릭 시 **즉시 DB가 0으로 바뀝니다.**
2.  마이닝 페이지에서 새로고침하거나 데이터가 동기화되면 **1022시간이 00:00:00으로, 누적 보상이 0 BW로** 깨끗하게 돌아갑니다.

## 5. 승인 요청
위 계획대로 `server/routes/admin.ts`를 수정하여 **진짜 초기화 기능**을 구현해도 되겠습니까?

**보고자:** Antigravity (2026.01.15)
